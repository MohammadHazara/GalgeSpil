<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.mh.galgespil.MainActivity">


    <TextView
        android:text="@string/welcome_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="133dp"
        android:id="@+id/textView"
        android:textSize="36sp"
        android:textAlignment="center" />

    <Button
        android:text="Start Spil"
        android:layout_width="200dp"
        android:layout_height="70dp"
        android:id="@+id/startGameButton"
        android:fontFamily="monospace"
        android:layout_below="@+id/textView"
        android:layout_alignStart="@+id/seOrdListeButton"
        android:layout_marginTop="56dp" />

    <Button
        android:text="Se Ordliste"
        android:layout_width="200dp"
        android:layout_height="70dp"
        android:layout_marginTop="95dp"
        android:id="@+id/seOrdListeButton"
        android:fontFamily="monospace"
        android:layout_alignTop="@+id/startGameButton"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
